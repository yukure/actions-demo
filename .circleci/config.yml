version: 2.1

orbs: 
  terraform: aws-cli/aws-cli@2.0.6

workflows:
  build-workflow:
    jobs:
      - build

jobs:
  aws-cli-example:
    executor: aws-cli/default
    steps:
      - checkout
      - aws-cli/setup
      - run:
          command: |
            aws --version
